# EXPERIMENTAL — see worker.py. The primary deploy path is the Dockerfile.
name = "bokehbowl"
main = "worker.py"
compatibility_date = "2026-07-01"
compatibility_flags = ["python_workers"]

# [[d1_databases]]
# binding = "DB"
# database_name = "bokehbowl"
# database_id = "<your-d1-database-id>"

# Secrets (set with `wrangler secret put`): SESSION_SECRET, ADMIN_PASSWORD,
# SMTP_USERNAME, SMTP_PASSWORD, MAIL_SENDER
